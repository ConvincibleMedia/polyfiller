import { analysePolyfills, PolyfillScanner } from './core/polyfill-scanner.js';
import { VersionedPolyfillRegistry } from './core/versioned-polyfill-registry.js';

export { analysePolyfills, PolyfillScanner, VersionedPolyfillRegistry };
export const analyzePolyfills = analysePolyfills;
